---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { Icon } from 'astro-icon/components';

const values = [
// ... (no changes here)
];

const stats = [
// ... (no changes here)
];

const team = [
  {
    name: "Raphaël Le Calvé",
    url: "https://www.linkedin.com/in/raphael-le-calv%C3%A9-a0a4b02a6/",
    photo: "/images/team/Raphael-Le-Calve_IMG_4438.jpg"
  },
  {
    name: "Fabrice Paget-Domet",
    url: "https://www.linkedin.com/in/fabrice-paget-domet-34ba205a/",
    photo: "/images/team/Fabrice-Paget-Domet_IMG_4513.jpg"
  },
  {
    name: "Coraline Paget-Domet",
    url: "https://www.linkedin.com/in/coraline-paget-domet-4027855a/",
    photo: "https://i.pravatar.cc/150?u=cpd"
  },
  {
    name: "Océane Neury",
    url: "https://www.linkedin.com/in/oc%C3%A9ane-neury-478137231/",
    photo: "https://i.pravatar.cc/150?u=on"
  },
  {
    name: "Mohamed Azroumbaze",
    url: "https://www.linkedin.com/in/mohamed-azroumbaze-5794769/",
    photo: "https://i.pravatar.cc/150?u=ma"
  },
  {
    name: "Matis Bot",
    url: "https://www.linkedin.com/in/matis-bot/",
    photo: "https://i.pravatar.cc/150?u=mb"
  },
  {
    name: "Loïc Nabédé",
    url: "https://www.linkedin.com/in/lo%C3%AFc-nab%C3%A9d%C3%A9/",
    photo: "/images/team/Photo LKD.jpg"
  },
  {
    name: "Sophie Alard",
    url: "https://www.linkedin.com/in/sophie-alard-51a57b56/",
    photo: "https://i.pravatar.cc/150?u=sa"
  },
  {
    name: "Paul Pillet",
    url: "https://www.linkedin.com/in/paul-pillet-8324411b2/",
    photo: "https://i.pravatar.cc/150?u=pp"
  }
];

const bars = [1, 2, 3, 4, 5, 6, 7, 8];
---

<Layout title="Accueil">
	<Header />
	
	<main class="pt-24">
		<!-- Hero Section -->
		<section class="relative min-h-[90vh] flex items-center overflow-hidden bg-primary py-20">
			<div class="absolute inset-0 z-0">
				<div class="absolute inset-0 bg-gradient-to-r from-primary/95 via-primary/85 to-primary/40 z-10"></div>
				<div class="w-full h-full bg-[url('https://images.unsplash.com/photo-1502602898657-3e91760cbb34?q=80&w=2073&auto=format&fit=crop')] bg-cover bg-center opacity-60 scale-105 animate-slow-zoom"></div>
			</div>
			
			<div class="relative z-20 max-w-7xl mx-auto px-6 lg:px-8 text-white w-full">
				<div class="max-w-4xl">
					<div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-accent/20 border border-accent/30 text-accent-light text-xs font-bold uppercase tracking-widest mb-10 animate-reveal">
						<span class="relative flex h-2 w-2">
							<span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-accent opacity-75"></span>
							<span class="relative inline-flex rounded-full h-2 w-2 bg-accent"></span>
						</span>
						Expertise Immobilière Parisienne
					</div>
					<h1 class="text-6xl lg:text-8xl font-serif font-bold mb-8 leading-[1.1] animate-reveal" style="animation-delay: 0.1s">
						L'expertise immobilière<br />
						<span class="text-accent italic">au service</span> d'une vision long terme.
					</h1>
					<p class="text-xl md:text-2xl text-slate-300 mb-12 leading-relaxed max-w-2xl font-light animate-reveal" style="animation-delay: 0.3s">
						Ovalto Immobilier, family office privé, investit dans l’immobilier tertiaire et résidentiel avec une exigence d'excellence et une vision patrimoniale.
					</p>
					<div class="flex flex-wrap gap-6 animate-reveal" style="animation-delay: 0.5s">
						<a href="/societes" class="group px-10 py-5 bg-accent hover:bg-accent-light text-primary font-bold rounded-sm transition-all transform hover:-translate-y-1 shadow-2xl shadow-black/30 text-center min-w-[240px] flex items-center justify-center gap-3">
							Nos Sociétés
              <Icon name="heroicons:chevron-right" class="w-5 h-5 group-hover:translate-x-1 transition-transform" />
						</a>
						<a href="/ovalto-immobilier.pdf" download class="group px-10 py-5 border-2 border-white/20 hover:border-white hover:bg-white/5 text-white font-bold rounded-sm transition-all text-center min-w-[240px] flex items-center justify-center gap-3">
							<Icon name="heroicons:document-arrow-down" class="w-5 h-5 group-hover:translate-y-1 transition-transform" />
              Présentation PDF
						</a>
					</div>
				</div>
			</div>

			<!-- Central Element: Vertical Bars (Animated) -->
			<div class="absolute bottom-0 right-0 w-1/2 h-full flex items-end justify-end p-12 lg:p-24 gap-4 lg:gap-8 opacity-20 pointer-events-none z-10">
				{bars.map((i) => (
					<div 
            class="w-4 lg:w-8 bg-accent animate-grow-up" 
            style={`height: ${i * 12}%; animation-delay: ${i * 0.1 + 0.5}s; box-shadow: 0 0 40px rgba(197, 160, 89, 0.2);`}
          ></div>
				))}
			</div>

      <!-- Background bar decor -->
      <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-accent/0 via-accent/50 to-accent/0 opacity-30"></div>
		</section>

		<!-- Stats Section with Scroll Animation -->
		<section class="py-20 bg-white border-b border-slate-100 relative">
			<div class="max-w-7xl mx-auto px-6 lg:px-8">
				<div class="grid grid-cols-2 md:grid-cols-4 gap-12">
					{stats.map((stat, idx) => (
						<div class="text-center group reveal-on-scroll" style={`transition-delay: ${idx * 0.1}s`}>
							<div class="text-4xl md:text-6xl font-serif font-bold text-primary mb-3 transition-all group-hover:text-accent group-hover:scale-110 flex items-center justify-center">
                {stat.value}<span class="text-accent text-2xl md:text-4xl ml-1">+</span>
              </div>
							<div class="text-[11px] text-slate-400 uppercase tracking-[0.3em] font-bold">{stat.label}</div>
              <div class="w-8 h-1 bg-accent mx-auto mt-6 transition-all group-hover:w-16 opacity-0 group-hover:opacity-100"></div>
						</div>
					))}
				</div>
			</div>
		</section>

		<!-- Vision Section - Enhanced with the bar motif -->
		<section id="vision" class="py-32 bg-white relative overflow-hidden">
      <!-- Signature Decor: Vertical bars on the side -->
      <div class="absolute left-0 top-1/2 -translate-y-1/2 h-2/3 flex flex-col gap-2 opacity-5 pointer-events-none">
        {bars.map((i) => (
          <div class="h-2 bg-primary" style={`width: ${i * 20}px`}></div>
        ))}
      </div>

			<div class="max-w-7xl mx-auto px-6 lg:px-8 relative z-10">
				<div class="grid grid-cols-1 lg:grid-cols-12 gap-20 items-center">
					<div class="lg:col-span-7 reveal-on-scroll">
						<div class="inline-block px-4 py-2 bg-primary/5 text-primary text-[10px] font-bold uppercase tracking-[0.4em] mb-10 rounded-sm border-l-4 border-accent">
              Notre ADN
            </div>
						<h3 class="text-5xl md:text-7xl font-serif font-bold text-primary mb-12 leading-tight">
							Un engagement <br/><span class="text-accent italic">multidimensionnel</span>.
						</h3>
						
            <div class="grid grid-cols-1 md:grid-cols-2 gap-10 mb-16">
              <div class="relative p-10 bg-slate-50 border border-slate-100 rounded-sm hover:bg-white hover:shadow-2xl hover:shadow-primary/5 transition-all duration-500 group overflow-hidden">
                <div class="absolute top-0 right-0 w-24 h-24 bg-primary/5 -mr-12 -mt-12 rounded-full group-hover:bg-primary/10 transition-colors"></div>
                <div class="w-14 h-14 bg-primary text-white flex items-center justify-center mb-8 shadow-lg shadow-primary/20">
                  <Icon name="heroicons:building-office" class="w-7 h-7" />
                </div>
                <h4 class="text-2xl font-serif font-bold text-primary mb-4">Immobilier d'Excellence</h4>
                <p class="text-slate-600 text-sm leading-relaxed">
                  Focus stratégique sur le tertiaire et le résidentiel premium au cœur de Paris et des Hauts-de-Seine.
                </p>
              </div>
              <div class="relative p-10 bg-slate-50 border border-slate-100 rounded-sm hover:bg-white hover:shadow-2xl hover:shadow-primary/5 transition-all duration-500 group overflow-hidden">
                <div class="absolute top-0 right-0 w-24 h-24 bg-accent/5 -mr-12 -mt-12 rounded-full group-hover:bg-accent/10 transition-colors"></div>
                <div class="w-14 h-14 bg-accent text-primary flex items-center justify-center mb-8 shadow-lg shadow-accent/20">
                  <Icon name="heroicons:globe-alt" class="w-7 h-7" />
                </div>
                <h4 class="text-2xl font-serif font-bold text-primary mb-4">Ecosystème Ovalto</h4>
                <p class="text-slate-600 text-sm leading-relaxed">
                  Une puissance financière et opérationnelle puisée dans la diversité : Sport, Événementiel et Terroir.
                </p>
              </div>
            </div>

						<div class="flex flex-col sm:flex-row items-start sm:items-center gap-10">
              <a href="https://ovalto.fr" target="_blank" class="group flex items-center gap-4 px-10 py-5 bg-primary text-white font-bold rounded-sm hover:bg-primary/90 transition-all shadow-xl shadow-primary/10">
                Découvrir le Groupe OVALTO
                <Icon name="heroicons:arrow-right" class="w-5 h-5 group-hover:translate-x-2 transition-transform" />
              </a>
              <div class="flex items-center gap-5">
                <div class="flex -space-x-3">
                  {team.map((member) => (
                    <a href={member.url} target="_blank" class="w-12 h-12 rounded-full border-4 border-white bg-slate-100 overflow-hidden shadow-sm hover:scale-110 hover:z-30 transition-all group/member relative" title={member.name}>
                      <img src={member.photo} alt={member.name} class="w-full h-full object-cover grayscale group-hover/member:grayscale-0 transition-all" />
                    </a>
                  ))}
                </div>
                <div class="text-[10px] text-slate-500 font-bold uppercase tracking-widest leading-loose">
                  <span class="block text-primary">Expertise Intégrée</span>
                  Asset & Investment Management
                </div>
              </div>
            </div>
					</div>
          
					<div class="lg:col-span-5 relative reveal-on-scroll">
            <!-- Main Image with Bar frame -->
						<div class="relative z-10 aspect-[3/4] overflow-hidden rounded-sm shadow-[40px_40px_0_0_rgba(22,46,91,0.05)]">
							<img src="/images/adn.jpg" alt="Architecture Ovalto" class="w-full h-full object-cover hover:scale-110 transition-transform duration-[2s]" />
						</div>
            
            <!-- Floating Data Card -->
            <div class="absolute -bottom-10 -right-6 lg:-right-12 z-20 bg-accent p-12 text-primary shadow-2xl animate-float">
              <div class="flex flex-col gap-1">
                <div class="text-7xl font-serif font-bold">40</div>
                <div class="text-[10px] font-black uppercase tracking-[0.3em] leading-none">Années de<br/>Savoir-Faire</div>
              </div>
              <!-- Bar motif on card -->
              <div class="flex gap-1 mt-6">
                {bars.map((i) => (
                  <div class="w-1 bg-primary/20" style={`height: ${i * 4}px`}></div>
                ))}
              </div>
            </div>

            <!-- Decorative background bars -->
            <div class="absolute -top-12 -left-12 z-0 opacity-10 flex gap-2">
              {bars.map((i) => (
                <div class="w-2 bg-primary" style={`height: ${i * 20}px`}></div>
              ))}
            </div>
					</div>
				</div>
			</div>
		</section>

		<!-- Valeurs - Reimagined as an interactive bar-grid -->
		<section class="py-32 bg-slate-50 relative overflow-hidden">
			<div class="max-w-7xl mx-auto px-6 lg:px-8">
				<div class="flex flex-col md:flex-row justify-between items-end mb-24 gap-10 reveal-on-scroll">
          <div class="max-w-2xl">
					  <h2 class="text-accent uppercase tracking-[0.4em] text-[10px] font-bold mb-6 flex items-center gap-4">
              <span class="w-12 h-1 bg-accent"></span>
              Nos Valeurs
            </h2>
					  <h3 class="text-5xl md:text-6xl font-serif font-bold text-primary leading-tight">La rigueur au service <br/>de l'excellence</h3>
          </div>
          <div class="max-w-xs text-slate-500 text-sm leading-relaxed italic border-l-4 border-accent pl-8 py-2">
            "Nous construisons des relations fondées sur la transparence et la performance durable."
          </div>
				</div>
				
				<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-px bg-slate-200 shadow-2xl reveal-on-scroll">
					{values.map((value, idx) => (
						<div class="bg-white p-12 hover:bg-primary transition-all duration-700 group relative overflow-hidden">
              <!-- Animated bar background on hover -->
              <div class="absolute bottom-0 left-0 w-full h-0 group-hover:h-full bg-primary transition-all duration-500 z-0"></div>
              
							<div class="relative z-10">
                <div class="w-16 h-16 bg-slate-50 flex items-center justify-center mb-10 group-hover:bg-accent group-hover:text-primary transition-all duration-500 shadow-sm">
                  <Icon name={value.icon} class="w-8 h-8" />
                </div>
                <h4 class="text-2xl font-serif font-bold text-primary mb-6 group-hover:text-white transition-colors">{value.title}</h4>
                <p class="text-slate-600 text-sm leading-relaxed group-hover:text-slate-300 transition-colors">{value.description}</p>
                
                <!-- Bar motif inside card -->
                <div class="flex gap-1 mt-10 opacity-20 group-hover:opacity-100 group-hover:text-accent transition-all">
                  {[1, 2, 3].map((i) => (
                    <div class="w-1 bg-current" style={`height: ${i * 8}px`}></div>
                  ))}
                </div>
              </div>
						</div>
					))}
				</div>
			</div>
		</section>

		<!-- Call to Action - The "Final Ascension" -->
		<section id="contact" class="py-40 bg-primary relative overflow-hidden">
      <!-- Mega Bar Motif Background -->
      <div class="absolute inset-0 flex items-end justify-center gap-12 opacity-5 pointer-events-none">
        {bars.map((i) => (
          <div class="w-24 bg-white" style={`height: ${i * 15}%`}></div>
        ))}
      </div>

			<div class="relative z-10 max-w-5xl mx-auto px-6 text-center reveal-on-scroll">
				<h2 class="text-6xl md:text-8xl font-serif font-bold text-white mb-10 leading-tight">Une expertise globale <br/><span class="text-accent italic">au service de l'immobilier</span>.</h2>
				<p class="text-xl md:text-2xl text-slate-300 mb-16 leading-relaxed font-light max-w-4xl mx-auto">
					Investissement pour compte propre, gestion, commercialisation et promotion immobilière : <br class="hidden md:block" /> nous maîtrisons chaque étape de la chaîne de valeur pour valoriser le patrimoine parisien.
				</p>
				<div class="flex flex-col sm:flex-row justify-center gap-8">
					<a href="mailto:contact@ovalto-immobilier.fr" class="group flex items-center justify-center gap-4 px-12 py-6 bg-accent text-primary font-bold rounded-sm hover:bg-accent-light transition-all shadow-2xl shadow-black/40 text-lg">
						<Icon name="heroicons:envelope" class="w-6 h-6" />
						contact@ovalto-immobilier.fr
            <Icon name="heroicons:arrow-long-right" class="w-5 h-5 group-hover:translate-x-2 transition-transform" />
					</a>
					<a href="tel:0184190240" class="flex items-center justify-center gap-4 px-12 py-6 border-2 border-white/20 text-white font-bold rounded-sm hover:border-white hover:bg-white/5 transition-all text-lg">
						<Icon name="heroicons:phone" class="w-6 h-6" />
						01 84 19 02 40
					</a>
				</div>
			</div>
		</section>
	</main>
	
	<Footer />
</Layout>

<script>
  // Intersection Observer for reveal animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('active');
      }
    });
  }, observerOptions);

  document.querySelectorAll('.reveal-on-scroll').forEach(el => observer.observe(el));
</script>

<style>
	@keyframes reveal {
		from { opacity: 0; transform: translateY(40px); filter: blur(15px); }
		to { opacity: 1; transform: translateY(0); filter: blur(0); }
	}
	@keyframes grow-up {
		from { transform: scaleY(0); transform-origin: bottom; }
		to { transform: scaleY(1); transform-origin: bottom; }
	}
  @keyframes float {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(2deg); }
  }
  @keyframes slow-zoom {
    from { transform: scale(1.05); }
    to { transform: scale(1.2); }
  }

	.animate-reveal {
		animation: reveal 1.4s cubic-bezier(0.16, 1, 0.3, 1) forwards;
		opacity: 0;
	}
	.animate-grow-up {
		animation: grow-up 1.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
	}
  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
  .animate-slow-zoom {
    animation: slow-zoom 20s ease-out infinite alternate;
  }

  /* Reveal on scroll utility classes */
  .reveal-on-scroll {
    opacity: 0;
    transform: translateY(30px);
    transition: all 1s cubic-bezier(0.16, 1, 0.3, 1);
    filter: blur(10px);
  }
  .reveal-on-scroll.active {
    opacity: 1;
    transform: translateY(0);
    filter: blur(0);
  }
</style>
